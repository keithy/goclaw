#!/bin/bash
#MISE description="Build the goclaw-sandbox Docker image"
set -euo pipefail

cd "$(dirname "${BASH_SOURCE[0]}")/.."

${DOCKER_CMD:-docker} build -t goclaw-sandbox:bookworm-slim -f Dockerfile.sandbox .
